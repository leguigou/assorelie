# Générer une valeur forte, par exemple :
# python -c "import secrets; print(secrets.token_urlsafe(48))"
MCP_API_KEY=replace-with-a-strong-random-secret
MCP_BASE_URL=https://mcp.assorelie.deloffre.fr
MCP_PORT=8921
ASSORELIE_DB_PATH=/app/data/assorelie.sqlite
ASSORELIE_SEED_DIR=/app/database/seeds
